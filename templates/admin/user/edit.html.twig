{% extends 'admin/layouts/base_admin.html.twig' %}

{% block title %}Редагування группи{% endblock %}

{% block body %}

    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>Редагування группи</h2>
        </div>
        <div class="col-lg-2">

        </div>
    </div>

    <div class="wrapper wrapper-content animated fadeInRight">
        <div class="row">
            <div class="col-lg-12">
                <div class="ibox ">
                    <div class="ibox-title">
                        <h5>Редагування группи<small>{{ group.name }}</small></h5>
                        <div class="ibox-tools">
                            <a href="{{ path('admin_group_index') }}">
                                <i class="fa fa-arrow-left"></i>
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">
                        {{ include('admin/group/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
